
model {
  # Likelihood
  for (i in 1:N) {
      Y[i] ~ dnorm(mu[i], tau)T(0,)
      mu.temp[i] <- c*(Tm-T[i])*(T[i]<Tm)
      mu[i] <- 0*(mu.temp[i]<0) + mu.temp[i]*(mu.temp[i]>0)
      
  }
  # Prior
  c ~ dgamma(1,10)
  Tm ~ dunif(25,45)
  sigma<-1/tau
  tau ~ dgamma(10, 1)

}
